# sample
sample_string = "1231"
print(sample_string)
print(len(sample_string))

# pep8

long_long_str = (
    "this is long long string"
    "writing in multiple line"
)

len(long_long_str)

name = "Manh"

pre_fix_name = "  To Nguyen Duc. "

group_name = f"this is my name {name} {pre_fix_name=}"

join_text = ", ".join([name, pre_fix_name])

lower = pre_fix_name.lower()

upper = pre_fix_name.upper()

title = pre_fix_name.title()

strip = pre_fix_name.strip()

type_of_str = type(pre_fix_name)

split_str = pre_fix_name.split(", ")

print("end game")